
//- pre #{JSON.stringify(processMan.htmlProgram)} 
-var buttonsContainer = "process_buttonsContainer"
-var programContainer = "process_programContainer"
-var dev = true
-var width = dev ? "25vw" : "100%"

//- 2025-10-16 Поки йде розробка ProcessManagerVue,
//-  використовується стара версія procManFull.pug

//- div.process-wrapper.process-container
//-   //- в цьому місці бцде розташовуватися proscessManager 
//- style.
//-   .process-wrapper {
//-     height: 95%;
//-     width: !{width};
//-     min-height: 300px;
//-     border: 1px solid grey;
//-     border-radius: 5px;
//-     padding: 5px;
//-     display: flex;
//-     flex-direction: column;
//-     /* Це магія Flexbox: елемент розтягується, заповнюючи простір, що залишився */
//-     flex-grow: 1; 
//-      overflow-y: auto; /* Дозволяє прокручувати контент, якщо він не вміщається */

//-   }
  

div.col.h-100.bordered.border-secondary
  div.row(id=buttonsContainer)
    //- div.col
  div.row 
    div.col(id=programContainer)     

script.
  var  processMan = {};

  processMan.myElementsRender = myElementsRender;
  processMan.homeUrl = `#{processMan.homeUrl}/`;
  processMan.editTaskUrl = `#{processMan.tasksManager.homeURL}/`;
  processMan.buttons = {};
  processMan.buttons.container =  document.getElementById(!{`"${buttonsContainer}"`}); 
  processMan.program = {};
  processMan.program.container = document.getElementById(!{`"${programContainer}"`});

script
  include ./js/processMan_Buttons.js
  include ./js/myElR_ClassElementStep.js
  include ./js/processMan_Main.js

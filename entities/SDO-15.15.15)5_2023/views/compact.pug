//- Файл рендерить компактну сторінку конкретної сутності для выдображення на полы печей
-var _lang = lang ? lang : 'ua'

h2 Компактна сторінка-бейдж печі 

h3 #{entity.shortName[_lang]}
a(role="button" class='btn btn-primary btn-sm' href=`/entity/${entity.id}`) 
  = `${{ua:"Детальніше",en:"Detalisation",ru:"Детально"}[_lang]}`



